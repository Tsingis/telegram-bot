{
  "name": "telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot",
  "main": "pulumi/index.ts",
  "scripts": {
    "deploy": "serverless deploy",
    "package": "serverless package",
    "prettier": "prettier --check 'pulumi/*.{js,ts}'",
    "prettier:fix": "prettier --write 'pulumi/*.{js,ts}'",
    "stack": "pulumi stack select ",
    "refresh": "pulumi refresh",
    "preview": "pulumi preview",
    "up": "pulumi up"
  },
  "dependencies": {
    "@pulumi/aws": "^7.5.0",
    "@pulumi/pulumi": "^3.191.0"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "serverless": "^4.18.0",
    "serverless-python-requirements": "^6.1.2",
    "typescript": "^5.9.2"
  }
}
