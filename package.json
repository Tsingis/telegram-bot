{
  "name": "telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot",
  "scripts": {
    "deps": "pipenv requirements > requirements.txt",
    "deploy": "serverless deploy",
    "package": "serverless package"
  },
  "devDependencies": {
    "serverless": "^4.15.1",
    "serverless-python-requirements": "^6.1.2"
  }
}
